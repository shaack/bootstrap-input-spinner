/**
 * Author: shaack
 * Date: 14.03.2018
 */

(function( $ ) {

    $.fn.InputSpinner = function( action ) {

        if ( action === "open") {
            // Open popup code.
        }

        if ( action === "close" ) {
            // Close popup code.
        }

    };

}( jQuery ));